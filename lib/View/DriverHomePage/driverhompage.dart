import 'package:flutter/material.dart';

class AliKarDriverHomePage extends StatelessWidget {
  const AliKarDriverHomePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text("AliKar"),
      ),
    );
  }
}
